name: Quality checks

on:
  push:
    branches: ['*']
  pull_request:
    branches: ['*']

jobs:
  typecheck:
    name: Typecheck
    steps:
      - uses: eas/checkout
      - uses: eas/install_node_modules
      - name: Run TypeScript typecheck
        run: npx tsc --noEmit
