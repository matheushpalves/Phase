name: Build development client (Android)

on:
  push:
    branches: ['master']

jobs:
  build_android:
    name: Build Android development client
    type: build
    params:
      platform: android
      profile: development
